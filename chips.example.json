{
  "_": [
    "summarize_chips.py 的配置模板。把这份复制到**数据根目录**下改名 chips.json，",
    "跑的时候就只剩 `python summarize_chips.py <根目录>` 一条命令，什么开关都不用记。",
    "命令行给了同名参数就以命令行为准。下划线开头的键会被忽略，可以拿来写备忘。",
    "★ 填进去的模块名/步骤名是真实名字，所以这份文件跟数据放在一起，不要提交。"
  ],

  "_scale": [
    "把结果折算到倍频点上：键是 `模块:类型`（类型＝pll/vco，两边都能写 *），值是倍数。",
    "频率与 Kvco ×倍数，相噪/杂散/IPN +20log10(倍数) dB；功率/压控电压/电流/温度不动。",
    "模块名＝文件名里类型关键字前面那一截（<模块>PLL_..., <模块>VCO_...）。",
    "配错或漏配都会被当场喊出来：脚本会拿簿子自己的 fLO_MHz / fVCO_MHz 除以实测频点，",
    "跟这里配的对不上就报 ⚠⚠。"
  ],
  "scale": {
    "A:pll": 4,
    "A:vco": 8,
    "B:vco": 2
  },

  "_groups": [
    "电流页的分组：一个键一张表。三种写法：",
    "  \"组名\": [步骤键…]                          一张表一个组，末尾一行合计",
    "  \"组名\": {\"keys\": [...], \"note\": \"...\"}     同上，note 挂在合计行的备注上",
    "  \"报告名\": {\"parts\": {\"部件名\": [...]}}       一张表分几块，每块一行合计，",
    "                                             末尾再来一行「报告名」＝各块合计相加",
    "第三种就是「某模块 总功耗 ＝ TCXO + LO path + 环内」那种报告。",
    "★ 不管配不配，最后**永远**还有一张「全部关断步骤」的全量表，分组是加出来的、不是替换。",
    "步骤键＝电流簿里步骤名那一列（默认第一列）的原样写法。"
  ],
  "groups": {
    "模块A 总功耗": {
      "parts": {
        "TCXO":     { "keys": ["步骤键1", "步骤键2"],
                      "note": "模块A 与 模块B 共用同一个 TCXO，两张表里都算了它，跨模块相加时只算一次" },
        "LO path":  ["步骤键3", "步骤键4"],
        "PLL 环内": ["步骤键5", "步骤键6", "步骤键7"]
      }
    },
    "模块B 总功耗": {
      "parts": {
        "TCXO":     ["步骤键1", "步骤键2"],
        "LO path":  ["步骤键8", "步骤键9"],
        "PLL 环内": ["步骤键10", "步骤键11"]
      }
    }
  },

  "_dsb": [
    "把**积分**相噪从单边带换算成双边带：+10log10(2)=3.01 dB，行名 IPN_SSB→IPN_DSB。",
    "逐点相噪与杂散不动——L(f) 与 dBc 的定义本来就是单边带，只有积分量才分 SSB/DSB",
    "（总相位误差 σ_φ² = ∫S_φ df = 2·∫L df）。命令行 --dsb / --no-dsb 可以盖掉这里。"
  ],
  "dsb": false,

  "_spec": [
    "汇总页上 Limit / Spec(Min/Typ/Max) / 仿真(Min/Typ/Max) 那七列的内容。",
    "**不用手写**：在生成出来的簿子上填好，然后",
    "    python spec_from_xlsx.py <填好的簿子.xlsx> --merge <这个文件>",
    "结构 = {页: {表键: {分组带: {测试项: {limit, spec:{min,typ,max}, sim:{…}}}}}}。",
    "表键 ＝ 那张表的大标题**去掉末尾（折算…）那截**——折算倍数或 SSB/DSB 换了，",
    "标题会变但表还是同一张，spec 不会因此丢；而口径真变了会当场喊一句",
    "（spec_meta.titles 记着填的时候标题长什么样）。",
    "对不上的行（表上没有这一行了）也会逐条报出来并猜一个最像的。"
  ],
  "spec": {
    "PLL_Summary": {
      "模块A PLL 性能汇总": {
        "Phase Noise": {
          "IPN_DSB": { "limit": "≤", "spec": { "max": -70.0 },
                       "sim": { "typ": -72.4 } }
        }
      }
    }
  },
  "spec_meta": {
    "titles": {
      "PLL_Summary": {
        "模块A PLL 性能汇总": "模块A PLL 性能汇总（折算 ×4：频率 ×4，相噪/杂散 +12.0 dB）"
      }
    }
  },

  "_module_alias": [
    "把认成两个的同一个模块并回去。模块名＝文件名里 PLL/VCO/Current 前面那一截，",
    "所以给某一份文件加个前缀（`补充_XXXPLL_...`）就会凭空多出一个模块，",
    "那颗芯片在原模块里**整块消失**（温巡页空出图位）。脚本会当场喊 ⚠⚠ 并把这行写给你。",
    "改文件名当然更好；不想动数据就写在这儿。左边写认出来的名字，右边写真名。"
  ],
  "module_alias": {
    "前缀_模块A": "模块A"
  },

  "_spur_targets": [
    "要报哪几个杂散频点（MHz）。**所有 Spur@ 行都是一回事**：值都从表尾那段",
    "仪器搜出来的清单里取、规则完全相同，26/52 也不例外。",
    "这里只需要写**模板没有声明**的那几个——模板声明过的（OtherSpurFreq1/2）",
    "每份簿子自己带着，自动就有；写重了会自动去重（六个全写上也行，出来一样）。",
    "唯一的差别在没有清单的时候：模板声明过的那几个还能退回模板那一格（会标出来），",
    "这里加的没有那个后备，就不进表。",
    "值只从表尾那段",
    "仪器搜出来的杂散清单里挑：每一行在「标称 ± 窗口」内取幅度最大的那一条，",
    "**这一行清单里没有这个分量就留空**（不是 0、也不是噪底）——所以同一列里",
    "有的行有数、有的行空着是正常的，跟仪器每次搜到几条有关。",
    "整份簿子一条都没搜到的那个频点直接不进表，并在控制台说一句。",
    "窗口默认 ±spur_tol，但彼此挨得近的（2/4/6 这种间隔 2 MHz）会自动收窄到",
    "不重叠为止，否则同一条杂散会同时进相邻两行。控制台会打收窄到多少，",
    "还会把「清单里有、但没被任何标称频点收走」的偏移列出来——要加哪个照着填。",
    "★ 还能写**频带**：`\"<1\"` ＝ 1 MHz 以内的杂散只报一行，取里面最大的那一条",
    "（近端那一串——参考泄漏/电源纹波/DBB 时钟边带——逐个报成行没人看得过来，",
    "评审要的就是一句『最近端最坏多少』）。频带行排在点频前面。",
    "还能按模块分开写: {\"模块A\": [\"<1\", 2, 4], \"模块B\": [\"<1\", 20], \"*\": [\"<1\"]}",
    "（键写错时会当场 ⚠⚠ —— 模块名是从文件名前缀认出来的，改文件名它就变了）。",
    "命令行 --spur-add \"<1,2,4,6,20\" 等价（PowerShell 里记得带引号，< 是重定向符）。"
  ],
  "spur_targets": ["<1", 2, 4, 6, 20],

  "_其余": "都对应同名命令行开关，不写就用默认值",
  "_chart_w": "每张图的宽度 cm（默认 15.5）。芯片竖条的列宽跟着等比撑开，永远放得下",
  "chart_w": 15.5,
  "modules": [],
  "chips": [],
  "ref_temp": 25,
  "op_vtune": null,
  "spur_tol": 2
}
